## 🚀 v1.1.0 — 안테나 자원 관리, 근무시간 개선, 성능 최적화

### ✨ 신규 기능

**지상국 안테나 자원 관리**
- 지상국별 동시 수신(RX) 안테나 대수 / 동시 커맨딩(TX) 채널 수 개별 설정 지원
- 안테나 점검 일정(시간대별 일시적 용량 변경) 등록 및 자동 반영
- 신규 UI: 지상국 관리 다이얼로그(`🛰️ Manage Stations`), 점검 일정 관리 다이얼로그(`🔧 Maintenance Schedule`)
- 패스 예측 단계에서 TX 채널 부족 가능성을 미리 알려주는 사전 경고 배너
- 가중치 기반 충돌 해결 화면에 지상국별 RX/TX 용량 및 점검 일정 여부 표시

**근무시간(Shift Hours) 규칙 개선**
- 근무시간 규칙을 지상국별로 다르게 적용 가능 (기존엔 전체 지상국 공통 규칙만 가능했음)
- 근무시간 밖 후보 때문에 그 시간대 전체가 비어버리던 문제 해결 → 근무시간 안의 차점자가 자동으로 승격되도록 개선

**Import / Export 강화**
- 외부 스케줄 파일 Import 시, 현재 설정된 지상국 용량을 초과하는 항목이 있으면 자동 경고
- Excel 내보내기에 "Station Capacity Info" 시트 추가 (지상국별 용량 및 점검 일정 기록)
- YAML 내보내기에 계산 당시의 지상국 용량 스냅샷 포함

### ⚡ 성능 개선

- 궤도 회차 번호 계산 로직을 벡터화하여 스케줄 계산 속도 대폭 개선 (1주일 분석 기준 약 18초 → 약 2초, **최대 9배 이상**)
- 계산 진행 상황을 실시간 진행률 표시줄로 확인 가능

### 🐛 버그 수정

- 안테나 슬롯에 여유가 있으면 위성별 Max Pass 상한이 무시되던 문제 수정
- Cross-Satellite Swap 성공 시 화면에 잘못된 위성의 시간 정보가 표시되던 문제 수정
- TX 사전 경고 및 가동률(%) 통계가 점검 일정을 반영하지 못하던 문제 수정
- 특정 조건에서 발생할 수 있었던 잠재적 크래시 지점 다수 사전 방어 처리
- 지상국 정보 파싱 중 존재하던 죽은 코드(비정상 문자열) 제거

### 🔧 기타

- `.gitignore` 정리: 개인 세션 설정(`config.json`), 실행 산출물, 임시 patch 파일 등이 저장소에 불필요하게 추적되지 않도록 개선

---

**하위 호환성**: 기존 지상국 설정 파일, 저장된 스케줄, config.json은 별도 마이그레이션 없이 그대로 사용 가능합니다.
